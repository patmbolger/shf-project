%h3 Events

%table.table.table-hover
  %thead
    %tr
      %th
        Event name
      %th
        Start Date
    %tr
  %tbody
    - events.each do |event|
      %tr.event
        %td.event-name
          = event.name
        %td.event-start-date
          = event.start_date


-# if the number of events > the TEASER_LIMIT...
%p.dinkurs_more Click here to see all DinKurs events for this company...
