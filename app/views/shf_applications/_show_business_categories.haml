-# This partial shows a members's business catgeories, and allows editing of subcategcories.
-#
-# The partial expects these locals:
-#   shf_application - a member's SHF application
-#

.categories
  .row.section-title
    .col.mt-2
      %h3 Business Categories:

  .row
    .col.mt-2
      .table-responsive
        %table.table.table-hover.table-sm

          %thead
            %th Category
            %th Subcategories
            %th
              %span.float-right Edit

          %tbody
            - shf_application.business_categories.roots.each do |business_category|

              = render 'shf_applications/business_category_display_row',
                       shf_application: shf_application,
                       business_category: business_category
